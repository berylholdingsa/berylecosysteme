# Risk Register

Source of truth: `security/risk_register.yaml`.

Controls are mapped to:
- security middleware (`src/core/security/middleware.py`)
- immutable audit chain (`src/core/audit/service.py`)
- Kafka compliance validators (`src/infrastructure/kafka/compliance/*`)
